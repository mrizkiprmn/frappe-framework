apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-long
  namespace: frapper
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: worker-long
      app.kubernetes.io/instance: worker-long
  template:
    metadata:
      labels:
        app.kubernetes.io/name: worker-long
        app.kubernetes.io/instance: worker-long
    spec:
      containers:
        - name: worker-long
          args:
          - bench
          - worker
          - --queue
          - long
          image: frappe/erpnext-worker:v13.32.0
          imagePullPolicy: IfNotPresent
          volumeMounts:
          - name: sites-dir
            mountPath: /home/frappe/frappe-bench/sites
          - name: logs
            mountPath: /home/frappe/frappe-bench/logs
      volumes:
        - name: sites-dir
          persistentVolumeClaim:
            claimName: frappe-data
            readOnly: false
        - name: logs
          persistentVolumeClaim:
            claimName: frappe-logs
            readOnly: false
